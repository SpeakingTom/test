#pragma once
#include<vector>
#define max(a,b)     (((a) > (b)) ? (a) : (b))
using namespace std;
static std::vector<int>w = { 1,2,3,4,6,8,9 };

vector<int> ShellSort(vector<int>arr) {
	vector<vector<int>>temp;
	int k = ;





}